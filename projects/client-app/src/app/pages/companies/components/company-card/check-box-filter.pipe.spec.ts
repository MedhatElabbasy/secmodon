import { CheckBoxFilterPipe } from './check-box-filter.pipe';

describe('CheckBoxFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new CheckBoxFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
